{{-- 
Buat sebuah fungsi PHP bernama reverseString($text) yang menerima input berupa string, 
lalu mengembalikan output berupa string yang sudah dibalik.
Contoh:
Input: "ERP"
Output: "PRE" 
--}}

<?php
function reverseString($text)
{
    // Tulis logika pembalikan string di sini
}

echo reverseString('ERP'); // Output: PRE
